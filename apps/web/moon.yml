# apps/web/moon.yml
type: 'application'
language: 'javascript'
platform: 'node'

tasks:
  # Servidor de desarrollo (moon run web:dev)
  dev:
    command: 'vite'
    local: true

  # Construcción para producción (moon run web:build)
  build:
    command: 'vite build'
    outputs:
      - 'dist'

  # Linting (moon run web:lint)
  lint:
    command: 'eslint .'