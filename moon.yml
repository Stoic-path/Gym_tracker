$schema: 'https://moonrepo.dev/schemas/project.json'

# Base configuration for root project
type: 'application'
language: 'unknown'

# Define Moonrepo to manage everything in these folders
workspace:
  projects:
    - 'apps/*'
    - 'services/*'
    - 'packages/*'
    - 'infra'